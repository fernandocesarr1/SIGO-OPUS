# SIGO - Configurações de Ambiente
# Copie este arquivo para .env e configure as variáveis

# ===========================================
# BANCO DE DADOS
# ===========================================

# Conexão PostgreSQL (Supabase ou local)
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sigo?schema=public"

# URL direta para migrações (necessário para Supabase)
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/sigo?schema=public"

# ===========================================
# SERVIDOR
# ===========================================

# Porta do servidor backend (padrão: 3001)
PORT=3001

# Ambiente (development | production)
NODE_ENV=development

# ===========================================
# SEGURANÇA
# ===========================================

# Domínios permitidos para CORS (separados por vírgula)
# Em produção, configure apenas os domínios autorizados
ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# Chave secreta para JWT (gerar com: openssl rand -base64 32)
# JWT_SECRET="sua_chave_secreta_aqui"

# ===========================================
# INTEGRAÇÕES (OPCIONAL)
# ===========================================

# Chave da API Gemini (para funcionalidades de IA)
# IMPORTANTE: Esta chave deve ser usada APENAS no backend
# Nunca expor no frontend
GEMINI_API_KEY=your_api_key_here
